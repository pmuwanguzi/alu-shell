#!/usr/bin/env bash
#This script displays content in the current directory in a list format where only part of the name after the first dash is displayed.
#!/usr/bin/env bash
# This script displays the contents of the current directory in a list format
# Only the part of the name after the first dash is displayed

for file in *; do
  if [[ -f "$file" && "$file" == *-* ]]; then
    # Next displays the part after the first dash
    echo "${file#*-}"
  fi
done
